# Workflow Templates

> **Purpose:** Optimized execution sequences for multi-step tasks, inspired by SEW (+12% LiveCodeBench via workflow auto-optimization).

Schema and protocol defined in `../README.md` (Workflow Auto-Optimization section).

## Available Templates

*None yet — first template created after workflow analysis of a multi-step task.*

## Template Lifecycle

1. **Candidate** — proposed from deviation analysis, stored with `[candidate]` tag
2. **Active** — promoted after 3 successful uses with consistent results
3. **Deprecated** — replaced by a superior variant, kept for lineage
